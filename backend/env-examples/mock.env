# Configuración para MODO MOCK (sin base de datos)
# Copia este archivo como .env en la raíz del backend para trabajo offline

# Sin base de datos (no requerida para mock)
# DATABASE_URL=""

# Puerto del servidor backend
PORT=3007

# Modo mock activado
NODE_ENV=mock
MOCK_MODE=true

# Configuración de red
HOST=0.0.0.0

# Debug activado para desarrollo
DEBUG_MODE=true