# Variables de entorno para FRONTEND (MODO COMPLETAMENTE OFFLINE)
# Copia este archivo como .env.local para trabajo sin backend

# Sin backend (datos simulados en frontend)
NEXT_PUBLIC_BACKEND_URL=""

# Configuración offline
NEXT_PUBLIC_APP_MODE=offline
NEXT_PUBLIC_DEBUG=true